{
  "name": "lambda-benchmarks-layer",
  "scripts": {
    "init:x64": "SHARP_IGNORE_GLOBAL_LIBVIPS=1 npm install --arch=x64 --platform=linux --production",
    "init:arm64": "SHARP_IGNORE_GLOBAL_LIBVIPS=1 npm install --arch=arm64 --platform=linux --production"
  },
  "dependencies": {
    "canvas": "~2.6.1",
    "sharp": "^0.29.3"
  }
}
